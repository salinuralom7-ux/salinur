{
  "name": "nearse-tests",
  "private": true,
  "type": "commonjs",
  "description": "Playwright harnesses for docs/index.html. Kept separate from the app, which has no build step and no dependencies at all.",
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
