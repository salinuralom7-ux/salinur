# The assembled canvas is a build output — 2.3 MB of editor payload wrapped
# around the artboards below. It is republished from these sources whenever
# the design changes, so there is nothing in it worth keeping in git.
mysheher-redesign.html
